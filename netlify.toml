[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# SPA routing — redirige todo al index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
